name: Regenerate SampleTrainings.csv

on:
  schedule:
    # Every Sunday at 00:00 GMT
    - cron: "0 0 * * 0"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  regenerate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Regenerate SampleTrainings.csv
        run: python mockdata.py

      - name: Commit and push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if [[ -n "$(git status --porcelain SampleTrainings.csv)" ]]; then
            git add SampleTrainings.csv
            git commit -m "chore: regenerate SampleTrainings.csv [skip ci]"
            git push
          else
            echo "No changes to SampleTrainings.csv."
          fi
